import { GetVideoThumbnailPipe } from './get-video-thumbnail.pipe';

describe('GetVideoThumbnailPipe', () => {
  it('create an instance', () => {
    const pipe = new GetVideoThumbnailPipe();
    expect(pipe).toBeTruthy();
  });
});
